# AI stack as code

An AI stack as code declares runtime tools, parsers, notebooks, and model dependencies in a reproducible environment. The environment gives human developers and coding agents the same command surface while preserving different trust boundaries.

A practical local pipeline should ingest source documents, build an index, and generate a brief with citations that point back to real chunks. Offline operation is important because teams need repeatable demos and regression tests without hosted model keys.
